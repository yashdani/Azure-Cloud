Python version used: Python 3.7

import the csv file in your Azure database name the table as 'earthqukae'

In app.py:
1. replace the connection string with your own Azure database connection string.

To run locally, run the following commands:

pip install -r requirements
python app.py